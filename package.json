{
  "name": "fivem-appearance",
  "version": "1.2.2",
  "repository": "git@github.com:pedr0fontoura/fivem-appearance.git",
  "author": "snakewiz",
  "license": "MIT",
  "scripts": {
    "build:game": "cd game && yarn && yarn build",
    "build:web": "cd web && yarn && yarn build",
    "format:game": "cd game && yarn && yarn format",
    "format:web": "cd web && yarn && yarn format",
    "format": "yarn format:game && yarn format:web",
    "build": "yarn build:game && yarn build:web",
    "release": "yarn build && node release.js"
  }
}
